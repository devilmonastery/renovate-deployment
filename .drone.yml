# Code generated by infracode. DO NOT EDIT.
# Edit source manifests, run `make generate`, and commit the generated artifacts.
kind: pipeline
type: kubernetes
name: default
platform:
  os: linux
steps:
  - name: fmt
    image: golang:1.26.4
    commands:
      - printf 'machine github.com\nlogin oauth2\npassword %s\n' "$GITHUB_TOKEN" > ~/.netrc
      - chmod 600 ~/.netrc
      - go version
      - go fmt ./...
    environment:
      GITHUB_TOKEN:
        from_secret: github_module_token
      GONOSUMDB: github.com/devilmonastery/*
      GOPRIVATE: github.com/devilmonastery/*
    when:
      event:
        - push
        - pull_request
        - custom
      branch:
        - main
  - name: vet
    image: golang:1.26.4
    commands:
      - printf 'machine github.com\nlogin oauth2\npassword %s\n' "$GITHUB_TOKEN" > ~/.netrc
      - chmod 600 ~/.netrc
      - go vet ./...
    environment:
      GITHUB_TOKEN:
        from_secret: github_module_token
      GONOSUMDB: github.com/devilmonastery/*
      GOPRIVATE: github.com/devilmonastery/*
    when:
      event:
        - push
        - pull_request
        - custom
      branch:
        - main
  - name: test
    image: golang:1.26.4
    commands:
      - printf 'machine github.com\nlogin oauth2\npassword %s\n' "$GITHUB_TOKEN" > ~/.netrc
      - chmod 600 ~/.netrc
      - go test -race -coverprofile=coverage.txt -covermode=atomic ./...
    environment:
      GITHUB_TOKEN:
        from_secret: github_module_token
      GONOSUMDB: github.com/devilmonastery/*
      GOPRIVATE: github.com/devilmonastery/*
    when:
      event:
        - push
        - pull_request
        - custom
      branch:
        - main
  - name: build
    image: golang:1.26.4
    commands:
      - printf 'machine github.com\nlogin oauth2\npassword %s\n' "$GITHUB_TOKEN" > ~/.netrc
      - chmod 600 ~/.netrc
      - go build ./...
    environment:
      GITHUB_TOKEN:
        from_secret: github_module_token
      GONOSUMDB: github.com/devilmonastery/*
      GOPRIVATE: github.com/devilmonastery/*
    when:
      event:
        - push
        - pull_request
        - custom
      branch:
        - main
  - name: generate-verify
    image: golang:1.26.4
    commands:
      - printf 'machine github.com\nlogin oauth2\npassword %s\n' "$GITHUB_TOKEN" > ~/.netrc
      - chmod 600 ~/.netrc
      - make generate-verify
    environment:
      GITHUB_TOKEN:
        from_secret: github_module_token
      GONOSUMDB: github.com/devilmonastery/*
      GOPRIVATE: github.com/devilmonastery/*
    when:
      event:
        - push
        - pull_request
        - custom
      branch:
        - main
  - name: buildkit-build
    image: docker.io/moby/buildkit:v0.26.2
    commands:
      - buildctl --addr tcp://buildkitd.homes.svc.cluster.local:1234 build --frontend dockerfile.v0 --local context=. --local dockerfile=. --exporter image --exporter-opt "name=registry.local.rothwell.us/devilmonastery/renovate-worker:${DRONE_COMMIT_SHA}" --exporter-opt push=true
    when:
      event:
        - push
      branch:
        - main
